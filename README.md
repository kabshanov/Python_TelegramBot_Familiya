# Проект: Telegram-бот календаря + Django (админка, API, экспорт)

**Автор:** Михаил Кабшанов
**GitHub:** kabshanov
**Telegram:** @kabmik

-----

## Оглавление

  * [1. Описание проекта](#1-описание-проекта)
  * [2. Возможности](#2-возможности)
  * [3. Стек технологий](#3-стек-технологий)
  * [4. Архитектура](#4-архитектура)
      * [Компоненты](#компоненты)
      * [Логика работы с Базой Данных](#логика-работы-с-базой-данных)
  * [5. Запуск проекта (Docker)](#5-запуск-проекта-docker)
      * [Требования](#требования)
      * [Шаг 1: Конфигурация](#шаг-1-конфигурация)
      * [Шаг 2: Сборка и запуск](#шаг-2-сборка-и-запуск)
      * [Шаг 3: Создание суперпользователя](#шаг-3-создание-суперпользователя)
      * [Управление контейнерами](#управление-контейнерами)
  * [6. Структура проекта](#6-структура-проекта)
  * [7. Команды бота](#7-команды-бота)
  * [8. Диалоги (FSM)](#8-диалоги-fsm)
  * [9. Модели данных (Django)](#9-модели-данных-django)
      * [TgUser](#tguser)
      * [Event (managed=False)](#event-managedfalse)
      * [Appointment](#appointment)
      * [BotStatistics](#botstatistics)
  * [10. Админ-панель](#10-админ-панель)
  * [11. API (REST API](#11-api-rest-api)
      * [Эндпоинты](#эндпоинты)
      * [Пример запроса](#пример-запроса)
  * [12. Экспорт](#12-экспорт)
  * [13. Автоматические тесты](#13-автоматические-тесты)
  * [14. Локальный запуск (для разработки)](#15-локальный-запуск-для-разработки)

-----

## 1\. Описание проекта

Этот учебный проект представляет собой комплексное приложение, объединяющее **Telegram-бота** для управления личным календарем и **веб-сервер Django** для администрирования, предоставления API и экспорта данных.

Бот позволяет пользователям создавать, просматривать, редактировать и удалять события, а также приглашать других пользователей на встречи. Веб-часть предоставляет административную панель для управления пользователями и просмотра статистики, а также REST API (на Django Rest Framework) для интеграции и эндпоинты для безопасного экспорта данных.

Проект полностью **контейнеризован с помощью Docker**, что обеспечивает быстрый и идентичный запуск всего стека (Бот + Веб + База Данных) одной командой.

-----

## 2\. Возможности

### 📲 Telegram-бот

  * **Управление событиями (CRUD):** Создание, просмотр, редактирование и удаление событий через диалоги (FSM).
  * **Регистрация:** Привязка Telegram-аккаунта к профилю в базе данных (`/register`, `/login`).
  * **Встречи:** Возможность приглашать других пользователей на события, управлять статусами приглашений (`/invite`).
  * **Публичность:** Возможность сделать событие "публичным", чтобы его могли видеть другие (`/share_event`).
  * **Экспорт:** Получение безопасных, временных ссылок на скачивание своего календаря в форматах CSV и JSON (`/export`, `/calendar`).
  * **Отмена:** Универсальная команда `/cancel` для выхода из любого диалога.

### ⚙️ Веб-панель (Django)

  * **Админ-панель:** Полноценный интерфейс (`/admin/`) для просмотра и управления пользователями (`TgUser`), встречами (`Appointment`) и статистикой (`BotStatistics`).
  * **REST API (DRF):** Набор эндпоинтов для интеграции с внешними системами (просмотр событий, управление встречами).
  * **Безопасный экспорт:** Эндпоинты `/export/csv/` и `/export/json/`, защищенные временным токеном, который генерируется через бота.
  * **Статистика:** Сбор и отображение суточной статистики по активности пользователей и событий.

-----

## 3\. Стек технологий

  * **Python 3.11+**
  * **Django 5.x:** (ORM, Admin, Views)
  * **Django REST Framework (DRF):** (Serializers, ViewSets, API)
  * **python-telegram-bot (v13.13):** (Updater, Dispatcher, Handlers)
  * **PostgreSQL 16:** (Основная база данных)
  * **psycopg2-binary:** (Драйвер для подключения Python к Postgres)
  * **Gunicorn:** (WSGI-сервер для запуска Django в Docker)
  * **Docker & Docker Compose:** (Контейнеризация и оркестрация всего приложения)
  * **Pytest & pytest-django:** (Автоматическое тестирование)

-----

## 4\. Архитектура

### Компоненты

Проект разделен на три независимых, но связанных компонента, которые запускаются в Docker:

1.  **`db` (Контейнер PostgreSQL):**

      * Официальный образ `postgres:16-alpine`.
      * Хранит все данные в "томе" (volume) `pgdata`, что **гарантирует сохранность данных** даже при перезапуске контейнеров.
      * При первом запуске автоматически выполняет скрипт `init.sql` для создания "ручных" таблиц (`users`, `events`).

2.  **`web` (Контейнер Django):**

      * Собирается из `Dockerfile.web`.
      * Запускает миграции (`manage.py migrate`) для создания таблиц Django (профили, встречи, статистика).
      * Запускает `collectstatic` для сбора статики админ-панели.
      * Запускает `gunicorn` для обслуживания веб-трафика (админка, API, экспорт).
      * Подключается к `db` по имени хоста `db`.

3.  **`bot` (Контейнер Telegram-бота):**

      * Собирается из `Dockerfile.bot`.
      * Запускает `bot.py`.
      * Подключается к `db` по имени хоста `db`.

### Логика работы с Базой Данных

Проект использует **гибридную модель** доступа к данным для разделения ответственностей:

  * **Прямой доступ (psycopg2):**
      * Бот (`bot.py` -\> `db.py`) использует прямые SQL-запросы для **записи** в "ручные" таблицы `users` и `events`. Это было сделано для упрощения логики бота.
  * **Django ORM:**
      * Веб-часть (`web`) использует ORM Django для **записи** в "свои" таблицы: `calendarapp_tguser`, `calendarapp_appointment`, `calendarapp_botstatistics`.
  * **"Мост" (managed=False):**
      * Ключевая особенность: модель Django `Event` объявлена с `Meta.managed = False`.
      * Это означает, что Django **не пытается** создавать или изменять таблицу `events`. Он использует эту модель только для **чтения** данных, которые бот записал напрямую.
      * Это позволяет Django (админке, API, экспорту) "видеть" события, созданные ботом.

-----

## 5\. Запуск проекта (Docker)

Это **рекомендуемый** и основной способ запуска проекта. Он автоматически настраивает базу данных, устанавливает зависимости и запускает все компоненты.

### Требования

  * **Docker Desktop** (или Docker Engine / Docker Compose на Linux).

### Шаг 1: Конфигурация

1.  **Клонируйте репозиторий:**

    ```bash
    git clone <your-repo-url>
    cd Python_TelegramBot
    ```

2.  **Создайте `bot_secrets.py`:**
    В **корневой папке** проекта создайте файл `bot_secrets.py` и впишите в него ваш токен:

    ```python
    # bot_secrets.py
    API_TOKEN = 'ВАШ_ТОКЕН' 
    ```

    *(`Dockerfile.bot` скопирует этот файл внутрь контейнера)*

3.  **Создайте `.env`:**
    В **корневой папке** проекта создайте файл `.env`. Он содержит **не** токен бота, а пароли и адреса для **базы данных**.

    ```ini
    # Пример файла .env

    # --- Настройки Postgres ---
    # (эти переменные читает контейнер 'db')
    POSTGRES_DB=calendar_db
    POSTGRES_USER=calendar_user
    POSTGRES_PASSWORD=calendar_password

    # --- Настройки для Django и Бота ---
    # (эти переменные читают 'web' и 'bot')
    DB_NAME=calendar_db
    DB_USER=calendar_user
    DB_PASSWORD=calendar_password
    DB_HOST=db
    DB_PORT=5432

    # --- Настройки Django ---
    DJANGO_SECRET_KEY=dev-secret-key-change-this
    DJANGO_DEBUG=1
    DJANGO_ALLOWED_HOSTS=*
    SITE_BASE_URL=http://localhost:8000
    EXPORT_TOKEN_MAX_AGE=900
    ```

### Шаг 2: Сборка и запуск

Выполните эти команды в терминале:

```bash
# 1. Собрать образы (может занять несколько минут)
docker compose build

# 2. Запустить все контейнеры в фоновом режиме (-d)
docker compose up -d
```

Docker автоматически:

1.  Запустит `db` и дождется, пока он станет `healthy`.
2.  Выполнит `init.sql` (создаст `users` и `events`).
3.  Запустит `web`, который выполнит миграции Django (создаст `calendarapp_...` таблицы) и запустит `gunicorn`.
4.  Запустит `bot`.

### Шаг 3: Создание суперпользователя

Чтобы получить доступ к админ-панели, создайте суперпользователя. Команда выполняется *внутри* уже запущенного `web` контейнера:

```bash
docker compose exec web python manage.py createsuperuser
```

Введите имя (`admin`), email и пароль.

**Готово\!**

  * Админ-панель доступна: **`http://localhost:8000/admin/`**
  * API доступно: **`http://localhost:8000/api/`**
  * Бот работает в Telegram.

### Управление контейнерами

  * **Посмотреть логи (что происходит внутри):**

    ```bash
    docker compose logs -f web     # Логи Django
    docker compose logs -f bot     # Логи Бота
    docker compose logs -f db      # Логи Базы Данных
    ```

    (Нажмите `Ctrl+C`, чтобы выйти из логов)

  * **Остановить все (сохранив данные):**

    ```bash
    docker compose stop
    ```

  * **Запустить остановленные:**

    ```bash
    docker compose start
    ```

  * **Полностью остановить и удалить контейнеры (данные в `pgdata` останутся):**

    ```bash
    docker compose down
    ```

  * **ПОЛНОСТЬЮ СБРОСИТЬ ПРОЕКТ (Удалить контейнеры И БАЗУ ДАННЫХ):**

    ```bash
    docker compose down -v
    ```

-----

## 6\. Структура проекта

```text
/Python_TelegramBot_Familiya
│
├── .env                    # Секреты для Docker (БД, Django)
├── .dockerignore           # Игнор-файл для Docker
├── docker-compose.yml      # Главный файл оркестрации
├── Dockerfile.web          # Инструкция по сборке Django
├── Dockerfile.bot          # Инструкция по сборке Бота
├── init.sql                # SQL-скрипт для "ручных" таблиц (users, events)
│
├── bot.py                  # Точка входа Telegram-бота
├── bot_secrets.py          # Секретный API_TOKEN для бота
├── db.py                   # Низкоуровневый модуль работы с psycopg2
├── requirements.txt        # Зависимости Python
│
├── tgapp/                  # Логика бота
│   ├── core.py             # Общие хелперы, "ленивые" подключения
│   ├── fsm.py              # Логика состояний (FSM)
│   ├── handlers_events.py  # Обработчики команд событий
│   └── handlers_appointments.py # Обработчики команд встреч
│
├── webapp/                 # Django-проект
│   ├── manage.py           # Утилита управления Django
│   │
│   ├── webapp/             # Конфигурация Django
│   │   ├── settings.py     # Глобальные настройки
│   │   ├── urls.py         # Корневой роутер (admin/, api/, /)
│   │   ├── wsgi.py         # Точка входа для Gunicorn
│   │   └── ...
│   │
│   └── calendarapp/        # Приложение "Календарь"
│       ├── models.py       # Модели (TgUser, Event, Appointment, BotStatistics)
│       ├── admin.py        # Настройки админ-панели
│       ├── views.py        # Вьюхи для экспорта (CSV/JSON)
│       ├── urls.py         # Роутер (export/, api/)
│       ├── api/            # Директория для REST API
│       │   ├── serializers.py # Сериализаторы DRF
│       │   ├── urls.py        # Роутер API
│       │   └── views.py       # Вьюсеты DRF
│       └── ...
│
└── tests/                  # Автоматические тесты
    ├── conftest.py
    └── test_*.py
```

-----

## 7\. Команды бота

Список команд, которые понимает бот:

  * `/start`, `/help` — Справка и меню.
  * `/register` — Регистрация/привязка TG-профиля.
  * `/create_event` — Создать событие (запускает диалог).
  * `/display_events` — Показать мои события.
  * `/read_event <id>` — Показать событие по ID.
  * `/edit_event [<id> <текст>]` — Изменить описание события.
  * `/delete_event [<id>]` — Удалить событие.
  * `/share_event` — Отметить событие как "публичное" (запускает диалог).
  * `/my_public` — Список моих публичных событий.
  * `/public_of` — Публичные события другого пользователя (запускает диалог).
  * `/invite` — Создать приглашение на встречу (запускает диалог).
  * `/login` — Привязка аккаунта (аналог /register).
  * `/calendar` — Показать личный календарь (через ORM) и кнопки экспорта.
  * `/export` — Получить кнопки со ссылками для выгрузки CSV/JSON.
  * `/cancel` — Отменить текущий диалог в любом месте.

-----

## 8\. Диалоги (FSM)

Бот использует простую FSM (Finite State Machine) для управления многошаговыми диалогами.

  * **CREATE**: `WAIT_NAME` -\> `WAIT_DATE` -\> `WAIT_TIME` -\> `WAIT_DETAILS`
  * **EDIT**: `WAIT_ID` -\> `WAIT_NEW_DETAILS`
  * **DELETE**: `WAIT_ID`
  * **SHARE**: `SHARE_WAIT_EVENT_ID`
  * **PUBLIC\_OF**: `WAIT_TG_ID`
  * **INVITE**: `INVITE_WAIT_PARTICIPANT` -\> `INVITE_WAIT_EVENT_ID` -\> `INVITE_WAIT_DETAILS`

-----

## 9\. Модели данных (Django)

Все модели ORM Django находятся в `webapp/calendarapp/models.py`.

### TgUser

  * Основная модель пользователя.
  * **`tg_id` (PK):** Telegram ID.
  * Хранит профильную информацию (`username`, `first_name`) и **счётчики** (`events_created`, `events_edited`, `events_cancelled`) для "личного кабинета" в админ-панели.

### Event (managed=False)

  * Это **виртуальная** модель-мост.
  * **`Meta.managed = False`**: Django **не** создает и **не** управляет этой таблицей.
  * **`db_table = "events"`**: Django "смотрит" на таблицу `events`, которую бот наполняет через `psycopg2`.
  * Используется для **чтения** в Админ-панели, API и при экспорте.

### Appointment

  * Модель для встреч.
  * Хранит `organizer_tg_id`, `participant_tg_id` и `status` (`pending`, `confirmed`, `cancelled`).
  * Использует логическую (нежесткую) связь с `Event`.

### BotStatistics

  * Модель для сбора суточной статистики.
  * Хранит `date` (уникальное) и счетчики (`user_count`, `event_count` и т.д.).

-----

## 10\. Админ-панель

Доступна по `/admin/` (после создания суперпользователя).

  * **Пользователи (TgUser):** Отображает список пользователей, их ID и счетчики активности.
  * **Встречи (Appointment):** Показывает все встречи, их статусы и участников.
  * **Статистика (BotStatistics):** Таблица со статистикой по дням.
  * **События (Event):** Список **только для чтения** всех событий из "ручной" таблицы `events`.

-----

## 11\. API (REST API)

Для интеграции с другими приложениями реализован REST API на Django Rest Framework.

  * **Browsable API:** `http://localhost:8000/api/`

### Эндпоинты

| Метод | URL | Описание |
| :--- | :--- | :--- |
| `GET` | `/api/events/public/` | Список **публичных** событий (с фильтрацией) |
| `GET` | `/api/users/` | Список профилей `TgUser` |
| `POST` | `/api/users/` | Создание профиля `TgUser` |
| `GET` | `/api/appointments/` | Список встреч (с фильтрацией) |
| `POST` | `/api/appointments/` | Создание новой встречи (статус `pending`) |
| `POST` | `/api/export/token/` | Получение временного токена для экспорта |

### Пример запроса

```bash
# Получить список публичных событий
curl -s http://localhost:8000/api/events/public/

# Создать встречу
curl -s -X POST http://localhost:8000/api/appointments/ \
  -H "Content-Type: application/json" \
  -d '{"organizer_tg_id": 123, "participant_tg_id": 456, "event_id": 10, "date": "2025-12-30", "time": "15:00:00"}'
```

-----

## 12\. Экспорт

  * **Получение ссылки:** Команда `/export` или `/calendar` в боте.
  * **Безопасность:** Ссылки защищены JWT-подобным токеном, который содержит `tg_user_id` и временную метку (TTL \~ 15 минут).
  * **Эндпоинты:**
      * `GET /export/json/?token=...` — выгрузка в JSON.
      * `GET /export/csv/?token=...` — выгрузка в CSV (адаптировано для Excel: кодировка `cp1251`, разделитель `;`).

-----

## 13\. Автоматические тесты

Проект включает набор автотестов для проверки критических узлов.

  * **Расположение:** `tests/`

  * **Технологии:** `pytest`, `pytest-django`

  * **Что покрыто:**

      * **Модели:** Создание `TgUser`, `Appointment`, `BotStatistics`.
      * **Экспорт:** Проверка, что эндпоинты `/export/` отдают корректный JSON и CSV.
      * **API:** Базовые проверки эндпоинтов.

  * **Запуск тестов (внутри Docker):**

    ```bash
    # Сначала убедитесь, что контейнеры запущены
    docker compose up -d

    # Запустить pytest внутри 'web' контейнера
    docker compose exec web pytest
    ```

    *Тесты используют отдельную **тестовую базу данных** (`test_calendar_db`), не затрагивая ваши рабочие данные.*

-----

## 14\. Локальный запуск (для разработки)

Этот способ **не рекомендуется** для финальной проверки, но полезен для отладки.

1.  **Установите Postgres** на свой компьютер.
2.  **Создайте БД:**
    ```sql
    CREATE USER calendar_user WITH PASSWORD 'calendar_password';
    CREATE DATABASE calendar_db OWNER calendar_user;
    ALTER USER calendar_user CREATEDB; -- Для запуска тестов
    ```
3.  **Создайте `bot_secrets.py`** (см. Шаг 1 в Docker).
4.  **Установите зависимости:**
    ```bash
    python -m venv .venv
    .venv\Scripts\activate
    pip install -r requirements.txt
    ```
5.  **Измените хост БД:**
      * В `db.py`: измените `host=os.getenv("DB_HOST", "db")` на `host=os.getenv("DB_HOST", "localhost")`.
      * В `webapp/webapp/settings.py`: измените `HOST`: `os.getenv("DB_HOST", "localhost")`.
6.  **Запустите миграции (Терминал 1):**
    ```bash
    python webapp/manage.py migrate
    python webapp/manage.py createsuperuser
    ```
7.  **Создайте "ручные" таблицы:**
      * Запустите `psql -U calendar_user -d calendar_db` и вручную выполните код из `init.sql` (чтобы создать `users` и `events`).
8.  **Запустите Django (Терминал 1):**
    ```bash
    python webapp/manage.py runserver
    ```
9.  **Запустите Бота (Терминал 2):**
    ```bash
    python bot.py
    ```