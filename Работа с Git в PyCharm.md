📘 **Работа с Git в PyCharm.md** —
практическое руководство для твоей среды, без академизма, но с акцентом на то, **как реально работать с ветками, пушами и мёрджами прямо в PyCharm**.
Ориентирован под твой текущий проект (`Python_TelegramBot_Kabshanov`), workflow с `main` и `bot-partX`.

---

```md
# Работа с Git в PyCharm
## Пошаговое руководство с пояснениями

---

## Зачем управлять Git прямо из PyCharm

PyCharm встроенно поддерживает весь Git-функционал:
- создание коммитов и пушей;
- работа с ветками;
- решение конфликтов;
- визуальный просмотр изменений.

Это удобнее, чем чистый терминал, особенно при частых фиксациях и код-ревью.

> Все действия в интерфейсе PyCharm — это те же команды `git ...`,  
> просто в графическом виде.

---

## 🔹 Первичная настройка Git в PyCharm

1. **Проверь, что Git подключен:**
   - Меню: `File → Settings → Version Control → Git`
   - Поле *Path to Git executable* должно указывать на путь к `git.exe`
     (PyCharm обычно определяет автоматически).

2. **Проверь, что твой проект под Git:**
   - В нижней панели появится надпись `Git: main` или `Git: bot-part2`.
   - Если нет, сделай:
     - Правый клик по проекту → `Git → Enable Version Control Integration...` → выбрать `Git`.

3. **Авторизация GitHub:**
   - `File → Settings → Version Control → GitHub → Add account`
   - Авторизуйся через браузер или токен.

---

## 🔹 Интерфейс Git в PyCharm

### Основные панели:

| Элемент | Где находится | Что делает |
|----------|----------------|-------------|
| **Git Branches** | Нижний-правый угол окна | Создание/переключение/слияние веток |
| **Commit window** | `Alt+0` или меню `Commit...` | Добавление файлов и создание коммитов |
| **Push/Pull** | Вверху справа или через `VCS → Git` | Отправка и получение изменений |
| **Log** | `View → Tool Windows → Git` → вкладка `Log` | История коммитов |
| **Diff Viewer** | Любой изменённый файл (Ctrl+D) | Сравнение текущего и последнего коммита |

---

## 🔹 Как создать коммит и отправить на GitHub

### 1. Сделай изменения в коде
PyCharm автоматически подсветит изменённые файлы:
- зелёный — новый файл;
- синий — изменённый;
- серый — неотслеживаемый (`untracked`).

### 2. Commit
1. Внизу нажми **Commit (Ctrl+K)**.
2. Отметь нужные файлы.
3. В поле комментария напиши, что сделал:
```

part3: добавлен FSM для /invite и проверка занятости

````
4. Нажми **Commit** (или **Commit and Push**, если хочешь сразу отправить).

### 3. Push (если не нажимал Commit and Push)
- В верхней панели выбери **Git → Push...**
- Подтверди список коммитов.
- Нажми **Push**.

> 💡 Если PyCharm спросит, связать ли ветку с origin — соглашайся.

---

## 🔹 Как переключаться между ветками

1. В правом нижнем углу — индикатор ветки:  
**`Git: main`**, **`Git: bot-part2`**, **`Git: bot-part3`**.

2. Кликни по нему:
- появится меню веток;
- можно:
  - **перейти** на другую (`Checkout`);
  - **создать новую** (`New Branch from...`);
  - **удалить** ненужную (`Delete`);
  - **слить** (`Merge into current`).

3. Для новой части проекта:
- `New Branch from main` → `bot-part3`.

PyCharm автоматически сделает:
```bash
git checkout -b bot-part3
````

---

## 🔹 Как слить ветку (Merge)

Когда ты завершил работу в ветке (например, `bot-part3`):

1. Переключись на основную:

   ```
   Git: main
   ```
2. Открой меню веток → выбери свою ветку (`bot-part3`) →
   **Merge into Current**.

PyCharm покажет дифф и спросит подтверждение. После слияния:

3. **Push** в GitHub (чтобы изменения появились там):

   ```bash
   git push
   ```

4. В браузере GitHub можно увидеть, что ветки совпадают.

---

## 🔹 Как сделать Pull (подтянуть изменения с GitHub)

Если ты работаешь с проекта на разных компьютерах или ветка обновилась:

1. В верхней панели: `Git → Pull...`
2. Выбери `origin/main` (или свою ветку).
3. Нажми **Pull**.

PyCharm:

* скачает изменения;
* автоматически объединит с твоими (если нет конфликтов).

---

## 🔹 Как решать конфликты

Если два человека (или ты на двух ПК) изменили один и тот же файл:

PyCharm покажет окно Merge Conflict:

* левая панель — твоя версия,
* правая — из GitHub,
* средняя — результат.

1. Используй кнопки **Accept Left / Accept Right / Accept Both**.
2. После решения:

   * нажми **Apply**,
   * сделай **Commit**,
   * потом **Push**.

---

## 🔹 Как посмотреть историю коммитов

Открой:

```
View → Tool Windows → Git → Log
```

Ты увидишь:

* кто, когда и что коммитил;
* стрелками — структуру веток;
* можно откатиться на старый коммит правым кликом → `Checkout`.

---

## 🔹 Как отменить изменения

| Ситуация                     | Действие                                                         |
| ---------------------------- | ---------------------------------------------------------------- |
| Изменил файл, но не коммитил | ПКМ → Git → Revert (или `Ctrl+Z`)                                |
| Уже коммитил, но не пушил    | `Git → Show History` → ПКМ → Reset Current Branch to Here → Soft |
| Коммитил и пушил             | Создай новый коммит, который исправляет ошибки                   |

> ⚠️ Не делай `hard reset`, если не уверен — можно потерять данные.

---

## 🔹 Как просмотреть изменения в файле

1. Правый клик на файле → `Git → Show Diff`.
2. Откроется сравнение «до/после»:

   * зелёный — добавлено;
   * красный — удалено;
   * серый — неизменено.

Можно откатывать отдельные строки (иконки слева).

---

## 🔹 Как удалить ветку

1. Правый клик на ветке в панели Git Branches.
2. Выбери **Delete**.
3. Если PyCharm спросит:

   ```
   "Branch 'bot-part2' is fully merged into main. Delete?"
   ```

   — нажми **Yes**.

---

## 🔹 Как создать Pull Request (через GitHub)

PyCharm показывает ссылку после пуша новой ветки:

> *"Create Pull Request"* → нажми.

Или вручную:

1. Открой репозиторий на GitHub.
2. Нажми **Compare & pull request**.
3. Укажи `base: main` и `compare: bot-part3`.
4. Напиши комментарий и нажми **Create Pull Request**.
5. После проверки — **Merge Pull Request**.

---

## 🔹 Как работать с .gitignore

Файл `.gitignore` указывает, что не пушить на GitHub:

```
.venv/
.idea/
*.sqlite3
bot_secrets.py
__pycache__/
```

> Добавляй туда всё, что не нужно другим — окружение, токены, временные файлы.

---

## 🔹 Быстрый рабочий цикл в PyCharm

| Шаг | Действие                                            |
| --- | --------------------------------------------------- |
| 1   | Изменяешь код                                       |
| 2   | Commit (Ctrl+K)                                     |
| 3   | Push (Ctrl+Shift+K)                                 |
| 4   | Проверяешь на GitHub                                |
| 5   | Pull перед началом новой сессии                     |
| 6   | При необходимости — новая ветка для следующей части |

---

## 🔹 Горячие клавиши Git в PyCharm

| Действие          | Клавиши                                     |
| ----------------- | ------------------------------------------- |
| Commit            | `Ctrl + K`                                  |
| Push              | `Ctrl + Shift + K`                          |
| Pull              | `Ctrl + T`                                  |
| Show History      | `Alt + 9` (или `View → Tool Windows → Git`) |
| Compare with HEAD | `Ctrl + D`                                  |
| New Branch        | `Alt + ~` → `Git Branches`                  |
| Merge             | `Alt + ~` → `Merge into Current`            |

---

## 🔹 Рекомендации

* Всегда **Pull перед началом дня** — чтобы быть в актуальной версии.
* Всегда **Commit часто** — чтобы история оставалась логичной.
* Всегда **Push в конце дня** — чтобы код был на GitHub (резервная копия).
* Не коммить `.venv`, `.idea`, токены, временные файлы.
* Проверяй перед пушем: `Commit diff` — PyCharm покажет все изменения.

---

## 🔹 Если PyCharm завис или ветка запуталась

1. Сохрани изменения вручную.
2. Открой терминал внизу и выполни:

   ```bash
   git status
   ```

   чтобы понять текущее состояние.
3. Если всё плохо — зафиксируй:

   ```bash
   git add .
   git commit -m "temp save before cleanup"
   ```
4. Переключись на нужную ветку, потом `git merge`.

PyCharm синхронизируется автоматически.

---

## 🔹 Проверка: твой проект готов к пушу

Перед коммитом убедись:

* `.gitignore` работает (`.idea/`, `.venv/` не подсвечены);
* PyCharm показывает ветку `bot-part3`;
* Все изменения зелёным в Commit-окне;
* Описание коммита читаемо и логично;
* Push прошёл без ошибок.

---

## 🔹 Пример идеального цикла (в твоём проекте)

1. Открыл PyCharm → проверил `Git: bot-part3`.
2. Pull (Ctrl+T).
3. Внёс изменения (`models.py`, `README.md`, `fsm.py`).
4. Commit (Ctrl+K) → `part3: docstrings, utils, invites FSM`.
5. Push (Ctrl+Shift+K).
6. GitHub → Pull Request → Merge.
7. Переключился обратно на `main` и `Pull`.

---

## 🔹 Мини-шпаргалка

| Действие        | Терминал          | PyCharm                   |
| --------------- | ----------------- | ------------------------- |
| Проверить ветку | `git branch`      | Правый нижний угол        |
| Commit          | `git commit -m`   | `Ctrl+K`                  |
| Push            | `git push`        | `Ctrl+Shift+K`            |
| Pull            | `git pull`        | `Ctrl+T`                  |
| Создать ветку   | `git checkout -b` | Git Branches → New branch |
| Merge           | `git merge`       | Git Branches → Merge      |
| Удалить ветку   | `git branch -d`   | Git Branches → Delete     |

---

## 🔹 Главное помнить

* **Коммит — это сохранение логического шага**, не просто «сохранил».
* **Ветка — это параллельная история**, не копия.
* **Push — это синхронизация с GitHub**, не просто отправка.
* **Pull — это обновление твоей локальной истории.**

> 💬 Работай в ветках, коммить часто, пушь в конце дня —
> это даёт контроль, безопасность и спокойствие при сдаче проекта.

---